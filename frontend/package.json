{
  "name": "frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:watch": "vite build --watch",
    "preview": "vite preview --port 5174"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
